import {
    createStyles
} from '@material-ui/core/styles';

const styles = () => createStyles({
    legend: {
        textAlign: 'right'
    },
    switch: {
        marginRight: 0
    }
});

export default styles;