import {
    createStyles
} from '@material-ui/core/styles';

const styles = () => createStyles({
    container: {
        marginTop: 20,
    },
    image: {
        maxWidth: '100%'
    }
});

export default styles;