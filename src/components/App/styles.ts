import {
    createStyles
} from '@material-ui/core/styles';

const styles = () => createStyles({
    root: {
        justifyContent: 'center',
        flexWrap: 'wrap',
    }
});

export default styles;